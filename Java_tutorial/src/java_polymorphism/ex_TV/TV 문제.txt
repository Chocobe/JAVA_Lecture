볼륨 제약조건: 1 ~ 12
채널 제약조건: 1 ~ 99

      // 1. 첫 번째 Television 객체를 만든다
      Television televisionTwo = new Television( );
      
      // 2. 두 번째 Television 객체의 브랜드 이름을 ‘삼성’로 한다	
      televisionOne.setBrandName("삼성");					
      
      // 3. 첫 번째 Television 객체의 전원을 켠다					
      televisionOne.setPowered(true);					
      
      // 4. 첫 번째 Television 객체의 채널 번호를 1번으로 한다					
      televisionOne.setChannel(1);					
      
      // 5. 첫 번째 Television 객체의 볼륨을 6으로 한다					
      televisionOne.setVolume(6);					
      
      // 6. 첫 번째 Television 객체의 모든 데이터 값들을 출력한다					
      System.out.println("텔레비젼 1\n"   + televisionOne.toString( ));					
      
      // 7. 첫 번째 Television 객체의 채널을 1만큼 감소시킨다					
      televisionOne.decrementChannel( );					
      
      // 8. 첫 번째 Television 객체의 볼륨을 1만큼 증가시킨다					
      televisionOne.incrementVolume( );					
      
      // 9. 첫 번째 Television 객체의 모든 데이터 값들을 출력한다					
      System.out.println("텔레비젼 1\n"   + televisionOne.toString( ));					
      
      

      // 1. 두 번째 Television 객체를 만든다					
      Television televisionTwo = new Television( );	
      				
      // 2. 두 번째 Television 객체의 브랜드 이름을 ‘엘지’로 한다					
      televisionTwo.setBrandName("엘지");					
      
      // 3. 두 번째 Television 객체의 전원을 켠다					
      televisionTwo.setPowered(true);					
      
      // 4. 두 번째 Television 객체의 채널 번호를 98번으로 한다					
      televisionTwo.setChannel(98);					
      
      // 5. 두 번째 Television 객체의 볼륨을 12로 한다					
      televisionTwo.setVolume(12);					
      
      // 6. 두 번째 Television 객체의 모든 데이터 값들을 출력한다					
      System.out.println("텔레비젼 2\n"   + televisionTwo.toString( ));									
      
      // 7. 두 번째 Television 객체의 채널을 1만큼 증가시킨다					
      televisionTwo.incrementChannel( );								
      
      // 8. 두 번째 Television 객체의 볼륨을 1만큼 증가시킨다					
      televisionTwo.incrementVolume( );					
      
      // 9. 두 번째 Television 객체의 모든 데이터 값들을 출력한다					
      System.out.println("텔레비젼 2\n"   + televisionTwo.toString( ));